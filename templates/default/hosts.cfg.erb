# Generated by Chef
# host definitions

<% @nodes.each do |n| -%>
define host {
  use server
  address <%= n['ipaddress'] %>
  host_name <%= n['hostname'] %>
<% if n.roles.nil? || n.roles.length == 0 -%>
  hostgroups all
<% else -%>
  hostgroups <%= n.roles.to_a.join(",") %>
<% end -%>
}

<% end -%>
